<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/img_frame_record"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- 완료 레이아웃 -->
    <LinearLayout
        android:id="@+id/completionLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="invisible">

        <TextView
            android:id="@+id/tvCompletionTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:text="장소포인트 추가완료"
            android:textColor="#000000"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvReward"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:text="일반 보상 아이템 1개 지급"
            android:textColor="#957343"
            android:textSize="16sp" />

        <Button
            android:id="@+id/btnDone"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:background="@drawable/img_btn_record"
            android:text="종료"
            android:textColor="#FFFFFF"
            app:backgroundTint="@null" />
    </LinearLayout>

    <!-- 포인트 장소 이름 입력 -->
    <LinearLayout
        android:id="@+id/nameInputLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/imagePreview"
            android:layout_width="250dp"
            android:layout_height="180dp"
            android:layout_marginBottom="16dp"
            android:scaleType="centerCrop"
            android:visibility="visible" />

        <View
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:background="#000000" />

        <EditText
            android:id="@+id/etPlaceName"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:enabled="true"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:hint="포인트 장소 이름 입력"
            android:padding="8dp"
            android:textColor="#000000"
            android:textColorHint="#9D7946"
            android:background="@null"
            android:gravity="center"
            tools:ignore="InefficientWeight" />

        <View
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:background="#000000" />

        <!-- 이름 입력 완료 버튼은 더 이상 필요 없으므로 제거됨 -->
    </LinearLayout>

    <!-- 사진 미리보기 -->


    <!-- 사진 관련 액션 버튼 -->
    <LinearLayout
        android:id="@+id/photoActionButtons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:visibility="visible">

        <Button
            android:id="@+id/btnClose"
            android:layout_width="100dp"
            android:layout_height="80dp"
            android:text="취소"
            android:textColor="#FFFFFF"
            android:visibility="visible"
            android:background="@drawable/img_btn_record"
            app:backgroundTint="@null"/>

        <Button
            android:id="@+id/btnConfirm"
            android:layout_width="100dp"
            android:layout_height="80dp"
            android:layout_marginStart="24dp"
            android:text="추가"
            android:background="@drawable/img_btn_record"
            app:backgroundTint="@null"/>
    </LinearLayout>

    <!-- 🔄 업로드 중 표시 -->
    <LinearLayout
        android:id="@+id/progressLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="16dp"
        android:visibility="invisible">

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/tvProgress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="로딩 중입니다..."
            android:textColor="#000000" />
    </LinearLayout>

</LinearLayout>
